{
    "php": {
        "version": "8.3"
    },
    "build_command": "npm install && npm run build",
    "start_command": "php artisan migrate --force && (php artisan storage:link --force || true) && _startup"
}
